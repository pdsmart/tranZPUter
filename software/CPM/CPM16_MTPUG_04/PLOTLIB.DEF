    

external procedure outp(B:byte);

external procedure micro2(comm:byte;x,y:integer);

external procedure plotwhere(var px,py:real);

external procedure plotline(penpos:integer;txpos,typos:real);

external procedure plotstring(px,py,height,theta:real;line:string);

external procedure plotsymbol(sym:integer;height:real);

external procedure plotarray(nopoints,freq,sym:integer;
    px,py,xmin,xmax,ymin,ymax,height,xlen,ylen:real;
    x,y:coord);
    
external procedure plotaxis(px,py,leng,theta,min,max,tic:real;name:string);

